sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t kubenaksang/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kubenaksang/docker-react npm run test
